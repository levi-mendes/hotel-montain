<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotelmontain">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelMontain">
        <activity android:name=".activities.listas.ListaReservasActivity"></activity>
        <activity android:name=".activities.listas.ListaQuartosActivity" />
        <activity android:name=".activities.listas.ListaFornecedoresActivity" />
        <activity android:name=".activities.cadastros.ReservaActivity" />
        <activity android:name=".activities.listas.ListaFuncionariosActivity" />
        <activity android:name=".activities.cadastros.QuartoActivity" />
        <activity android:name=".PesquisaGeralActivity" />
        <activity android:name=".activities.cadastros.FuncionarioActivity" />
        <activity android:name=".activities.cadastros.FornecedorActivity" />
        <activity android:name=".activities.cadastros.HospedeActivity" />
        <activity android:name=".activities.listas.ListaHospedeActivity" />
        <activity android:name=".activities.MenuActivity" /> <!-- <activity android:name=".LoginActivity"></activity> -->
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>